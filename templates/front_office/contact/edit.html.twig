{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Contact</h1>

    {{ include('front_office/contact/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('contact_index') }}">back to list</a>

    {{ include('front_office/contact/_delete_form.html.twig') }}
{% endblock %}
